local arr = require("efficientArray")

return arr {
    "About",
    "assets",
    "stylesheets",

    "built-info",
    "api",

    "CNAME",    -- not required (file, not folder)
    "README.md" -- not required (file, not folder)
}
